## 0.0.3

* Add the generic `download_file.dart` helper (`downloadFile`,
  `DownloadFileInfo`), on top of `tekaly_file_download_web` (web) and
  `tekaly_file_picker_flutter` (save dialog elsewhere).
* `downloadImage` now delegates to `downloadFile`, `file_picker` is no longer
  used directly.
* `filenameMimeType` and `anchorSelectorSetDownloadFileInfo` are re-exported.
* Playground: `flutter_packages/file_download_flutter_example`.

## 0.0.1

* TODO: Describe initial release.
