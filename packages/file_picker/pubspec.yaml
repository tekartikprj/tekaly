name: tekaly_file_picker
description: File picker interface (no flutter, no io, no web) with an in memory implementation
version: 0.2.0
publish_to: none

environment:
  sdk: ^3.12.0

dev_dependencies:
  test: ">=1.31.0"
  tekartik_lints:
    git:
      url: https://github.com/tekartik/common.dart
      path: packages/lints
resolution: workspace
